import { Request, Response } from "express";
import { InstitutionModel } from "../../database/models/Institution";

const registerInstitution = async (req: Request, res: Response) => {};

export default registerInstitution;
